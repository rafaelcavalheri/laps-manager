# ========================================
# CONFIGURAÇÃO DO SISTEMA LAPS
# ========================================
# Copie este arquivo para .env e configure conforme sua necessidade

# ========================================
# CONFIGURAÇÕES DO BANCO DE DADOS
# ========================================
# Host do banco de dados (interno do Docker)
DB_HOST=db

# Usuário do banco de dados
# IMPORTANTE: Use 'root' para funcionamento padrão
# Se alterar, certifique-se de que o usuário tem privilégios completos
DB_USER=root

# Senha do usuário do banco de dados
DB_PASSWORD=ALTERAR SENHA

# Nome do banco de dados
DB_NAME=laps

# ========================================
# CONFIGURAÇÕES DO MYSQL
# ========================================
# Senha do usuário root do MySQL
MYSQL_ROOT_PASSWORD=ALTERAR SENHA

# Nome do banco de dados (deve ser igual ao DB_NAME)
MYSQL_DATABASE=laps

# ========================================
# CONFIGURAÇÕES DO CONTAINER WEB
# ========================================
# Porta onde a aplicação ficará disponível
WEB_PORT=8081

# Nome da imagem Docker
WEB_IMAGE=laps-interface

# ========================================
# CONFIGURAÇÕES DE FUSO HORÁRIO
# ========================================
# Fuso horário (exemplos: America/Sao_Paulo, Europe/London, UTC)
TZ=America/Sao_Paulo

# ========================================
# CONFIGURAÇÕES DO LDAP
# ========================================
# Servidor LDAP (exemplo: ldap://seu-servidor-ldap.local)
LDAP_SERVER=ldap://seu-servidor-ldap.local

# Usuário LDAP (DN completo - exemplo)
LDAP_USER=CN=usuario-laps,OU=TI,DC=exemplo,DC=local

# Senha do usuário LDAP
LDAP_PASSWORD=ALTERAR SENHA LDAP

# Base DN para busca (exemplo)
LDAP_BASE=DC=exemplo,DC=local